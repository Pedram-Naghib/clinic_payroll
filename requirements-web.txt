# Web app stack (FastAPI + Postgres). Separate from requirements.txt, which
# is the PySide6 desktop app's dependencies -- the two don't need to share
# a virtualenv or a Docker image.
fastapi==0.139.2
uvicorn[standard]==0.51.0
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0
psycopg[binary]==3.3.4
sqlalchemy==2.0.51
alembic==1.18.5
bcrypt==5.0.0
slowapi==0.1.9
